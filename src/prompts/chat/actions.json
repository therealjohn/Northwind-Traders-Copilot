[
    {
        "name": "createOrder",
        "description": "creates a new order to restock an item and returns the order ID.",
        "parameters": {
            "type": "object",
            "properties": {
                "productId": {
                    "type": "string",
                    "description": "The product ID of the item to order."
                },
                "quantity": {
                    "type": "integer",
                    "description": "The number of the product to order."
                }
            },
            "required": [
                "productId",
                "quantity"
            ]
        }
    },
    {
        "name": "getOrder",
        "description": "returns the order details for a given order ID.",
        "parameters": {
            "type": "object",
            "properties": {
                "orderId": {
                    "type": "string",
                    "description": "The order ID to retrieve."
                }
            },
            "required": [
                "orderId"
            ]
        }
    },
    {
        "name": "cancelOrder",
        "description": "cancels an order and returns the order ID.",
        "parameters": {
            "type": "object",
            "properties": {
                "orderId": {
                    "type": "string",
                    "description": "The order ID to cancel."
                }
            },
            "required": [
                "orderId"
            ]
        }
    },
    {
        "name": "getProduct",
        "description": "return a product's details for a given product name.",
        "parameters": {
            "type": "object",
            "properties": {
                "productName": {
                    "type": "string",
                    "description": "The product name to retrieve."
                }
            },
            "required": [
                "productName"
            ]
        }
    }
]